/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // image: {
  //   domains: ["images.unsplash.com"],
  // },
};

export default {
  images: {
    domains: ["images.unsplash.com"],
  },
};
